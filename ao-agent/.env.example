ANTHROPIC_API_KEY=sk-ant-votre-cle-ici

# Optionnel : URL du webhook Teams (voir README pour la configuration côté Teams).
# Si absent, les notifications sont simplement désactivées.
TEAMS_WEBHOOK_URL=
